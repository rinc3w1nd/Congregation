"use strict";
/*
 * CONGREGATION — town.js (Phase 3 builds the real Marrow Bay; this stub
 * holds the API surface so the engine wires up cleanly.)
 * API: TOWN.build(el) · TOWN.setStage(n) · TOWN.refresh(state)
 */

var TOWN = {
  build(el) {
    el.innerHTML = '<div id="town-stub">MARROW BAY<br><small>the town is being drawn — Phase 3</small></div>';
  },
  setStage(n) {},
  refresh(state) {},
};
